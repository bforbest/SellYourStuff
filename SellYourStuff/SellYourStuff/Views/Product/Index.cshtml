@model IEnumerable<SellYourStuff.Models.Product>

@{
    ViewBag.Title = "Index";
}
@*<div class="well-lg">
    <div class="row">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
               <div class="col-md-6">
                <div class="input-group">
                    <input name="searchString" type="text" class="form-control" placeholder="Search" aria-describedby="basic-addon1">
                    <div class="input-group-btn">
                        <button type="submit" class="btn btn-default"><span class="glyphicon glyphicon-search"></span></button>

                    </div>
                    @Html.DropDownList("SelectedCategory", null, "All Products", htmlAttributes: new { @onchange = "form.submit();", @class = "form-control" })
                </div> /input-group 
            </div>
            }
        </div>
</div>*@
<div class="row">
    @foreach (var item in Model)
    {

        <div class="col-sm-6 col-md-3">
            <div class="thumbnail">
                <img src="@Html.DisplayFor(modelItem => item.Image)" alt="..." onerror="this.src = 'http://placehold.it/350x250'">
                <div class="caption">
                    @if (item.Title.Length > 15)
                    {
                    <h3>@item.Title.Substring(0, 15) ...</h3>
                    }
                    else
                    {
                        <h3>@item.Title</h3>
                    }
                    <p>@(item.Description.Substring(0, 30))</p>
                    <p> <a href="@Url.Action("Details", new { id=item.Id })" class="btn btn-default" role="button">Visa Produkt</a></p>
                </div>
            </div>
        </div>

    }
</div>



